global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'llm_shadow_framework'
    static_configs:
      # host.docker.internal allows Prometheus inside Docker to scrape the FastAPI app on the host machine
      - targets: ['host.docker.internal:8000']
